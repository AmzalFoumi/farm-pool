import { PlaceholderScreen } from "@/components/app/placeholder-screen";

/* Replaced in the next gate by the real grid/list browse screen. It exists now
   so the five-tab shell has a route behind every trigger. */
export default function ListingsScreen() {
  return <PlaceholderScreen title="Listings" note="Produce listings will appear here." />;
}
