node_modules/
package-lock.json

# Expo / build output
.expo/
dist/
web-build/
mobile/ios/
mobile/android/

# Generated by Uniwind, which rewrites it in its own style (single quotes,
# 4-space indent, no semicolons) on every `expo start` and `expo export`.
# Prettier reformatting it back on commit only made the file churn between the
# two, so it is left in whatever shape the generator last wrote. It is tracked,
# not gitignored, so `npx tsc --noEmit` works on a fresh clone before anyone has
# run the app. Its own first line says not to edit it by hand.
mobile/uniwind-types.d.ts

# Planning prose is hand-wrapped for readability; leave it alone.
.plans/
.plans.local/
CLAUDE.md
CLAUDE.local.md
AGENTS.md
README.md
