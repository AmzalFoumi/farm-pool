import { Controller } from '@nestjs/common';

/**
 * HTTP entry points for the coordination domain. No routes yet — add handlers here and
 * delegate straight to an application service. Keep domain rules out of this file.
 */
@Controller('coordination')
export class CoordinationController {}
